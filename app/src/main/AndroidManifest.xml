<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="smt3.progdevelopment.yummyproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yummyproject">
        <activity android:name=".User.registUser"></activity>
        <activity android:name=".Admin.registAdmin" />
        <activity android:name=".Admin.loginAdmin" />
        <activity android:name=".User.loginUser" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LandingPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>